# Produtos — Catálogo

Resumo: gerenciamento de produtos com `name`, `price`, `description` e `unit`.

Arquivos chave
- `src/pages/Products.tsx`
- `src/components/forms/ProductForm.tsx`

Uso em Vendas
- Produtos são selecionados em `SaleForm` e usados para montar `sale_items`.
